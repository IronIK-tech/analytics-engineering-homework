# Homework 2 — Data Sources

Connecting to multiple data sources running via Docker Compose.

## Stack
PostgreSQL · MongoDB · Redis · MinIO

## Tasks
1. Start all services with Docker Compose
2. Connect to each service inside the container and run basic read/write operations
3. Verify data via DataGrip
